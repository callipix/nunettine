<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.pro.join.mapper.ProJoinMapper">

	<select id="proLogin" parameterType="kr.or.ddit.vo.UsersVO" resultType="int">
		SELECT COUNT(*) FROM USERS WHERE USER_ID=#{userId} AND USER_PASSWORD=#{userPassword}
	</select>
	
</mapper>